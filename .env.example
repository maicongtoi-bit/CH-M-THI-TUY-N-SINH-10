# =========================
# Gemini API Key
# =========================
# Dùng cho môi trường local (copy file này thành .env.local và điền key thật)
# Trên Vercel: set Environment Variables thay vì commit key vào repo

GEMINI_API_KEY=your_gemini_api_key_here

# (Tuỳ chọn) Nếu bạn muốn theo chuẩn Vite (import.meta.env.*) thì dùng biến có tiền tố VITE_
# Khi đó trong code sẽ đọc: import.meta.env.VITE_GEMINI_API_KEY
VITE_GEMINI_API_KEY=your_gemini_api_key_here
